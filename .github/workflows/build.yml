name: Build APK

on:
  push:
    branches: [ main, master ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2

      # 提取版本号和日期
      - name: Extract Version
        id: version
        run: |
          VERSION=$(grep "versionName" app/build.gradle | head -1 | grep -oP '"\K[^"]+')
          BUILD_DATE=$(date +%Y%m%d)
          TAG_NAME="v${VERSION}-${BUILD_DATE}"
          echo "version=${VERSION}" >> $GITHUB_OUTPUT
          echo "build_date=${BUILD_DATE}" >> $GITHUB_OUTPUT
          echo "tag_name=${TAG_NAME}" >> $GITHUB_OUTPUT
          echo "Version: ${VERSION}, Build Date: ${BUILD_DATE}, Tag: ${TAG_NAME}"

      - name: Build Release APK
        run: gradle assembleRelease

      - name: Build Debug APK
        run: gradle assembleDebug

      - name: Upload Release APK to Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: DBTOOL-release
          path: app/build/outputs/apk/release/*.apk

      - name: Upload Debug APK to Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: DBTOOL-debug
          path: app/build/outputs/apk/debug/*.apk

      # 检查tag是否已存在
      - name: Check if tag exists
        id: check_tag
        run: |
          if git rev-parse "${{ steps.version.outputs.tag_name }}" >/dev/null 2>&1; then
            echo "tag_exists=true" >> $GITHUB_OUTPUT
          else
            echo "tag_exists=false" >> $GITHUB_OUTPUT
          fi

      # 创建新tag（如果不存在）
      - name: Create Tag
        if: github.event_name == 'push' && !startsWith(github.ref, 'refs/tags/') && steps.check_tag.outputs.tag_exists == 'false'
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git tag -a "${{ steps.version.outputs.tag_name }}" -m "Automated release: ${{ steps.version.outputs.tag_name }}"
          git push origin "${{ steps.version.outputs.tag_name }}"

      # 自动创建Release（当push到main/master时）
      - name: Create Release on Push
        if: github.event_name == 'push' && !startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.version.outputs.tag_name }}
          name: DBTOOL ${{ steps.version.outputs.tag_name }}
          files: |
            app/build/outputs/apk/release/*.apk
            app/build/outputs/apk/debug/*.apk
          draft: false
          prerelease: false
          body: |
            # DBTOOL ${{ steps.version.outputs.tag_name }}
            
            ## 📦 发布信息
            - **版本**: ${{ steps.version.outputs.version }}
            - **构建日期**: ${{ steps.version.outputs.build_date }}
            - **提交**: ${{ github.sha }}
            - **分支**: ${{ github.ref_name }}
            
            ## 📥 下载
            - `app-release.apk` - Release版本（推荐用于生产环境）
            - `app-debug.apk` - Debug版本（用于开发测试）
            
            ## 🚀 安装方法
            ```bash
            adb install -r app-release.apk
            ```
            
            ## 📝 更新日志
            查看 [CHANGELOG.md](https://github.com/xbrooke/DBTOOL/blob/main/CHANGELOG.md) 了解详细更新内容
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # 创建正式Release（当创建版本标签时）
      - name: Create Release on Tag
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v1
        with:
          files: |
            app/build/outputs/apk/release/*.apk
            app/build/outputs/apk/debug/*.apk
          draft: false
          prerelease: false
          body: |
            # DBTOOL ${{ github.ref_name }}
            
            ## 📦 发布信息
            - **版本**: ${{ github.ref_name }}
            - **提交**: ${{ github.sha }}
            - **发布时间**: ${{ github.event.head_commit.timestamp }}
            
            ## 📥 下载
            - `app-release.apk` - Release版本（推荐）
            - `app-debug.apk` - Debug版本
            
            ## 🚀 安装
            ```bash
            adb install -r app-release.apk
            ```
            
            ## 📝 更新日志
            查看 [CHANGELOG.md](https://github.com/xbrooke/DBTOOL/blob/main/CHANGELOG.md) 了解详细更新内容
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
